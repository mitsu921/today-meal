# 가게별 사진 연결 준비본

이 패키지는 등록된 사진을 가게별로 연결하는 기능입니다. 사진 수집 서비스나 Google Places 연동을 완료한 버전은 아닙니다. 설정 없이 배포하면 기존 분류 그림이 나옵니다.

## 교체 파일
- 프로젝트 루트의 api/nearby.js
- app/index.html
- app/eatout-design.css

app 폴더에는 이전 이미지 자산도 포함했습니다. 기존 KAKAO_REST_API_KEY 설정은 유지하세요. 다른 서버 파일은 교체할 필요가 없습니다.

## 사진 등록
본인 소유이거나 사용 권한이 있는 사진을 HTTPS로 제공하고, 서버 환경변수 PLACES_PHOTO_MAP_JSON에 가게별 정보를 등록합니다. 키는 카카오 place ID입니다. API 응답에 id 필드를 추가했으므로 해당 가게의 실제 ID를 확인해 사용하세요. 이름이 비슷한 다른 지점의 사진을 자동 배정하지 않습니다.

형식 예시(아래 ID와 URL은 예시이며 그대로 적용하지 마세요):

```json
{"실제_카카오_장소_ID":{"url":"https://본인사진호스트/가게사진.jpg","credit":"사진 제공자","creditUrl":"https://제공자페이지"}}
```

사진은 등록한 가게에만 표시됩니다. 미등록 가게는 분류 이미지, 사진 로딩 실패 시에도 분류 이미지로 복귀합니다. 사진 출처는 제공된 경우 함께 표시합니다. 이 설정은 룰렛 분류 그림을 바꾸지 않습니다. 룰렛은 이름/전체 분류 기반 그림을 계속 사용합니다.

## 자동 사진 서비스
카카오 로컬 검색에는 사진 필드가 없습니다. Google Places 등 별도 사진 공급원을 사용하려면 서비스 활성화와 비용 설정, 출처 표시와 데이터 사용 조건을 반영한 추가 서버 연결이 필요합니다. 이 패키지는 Google API를 호출하지 않습니다.

## 검증
테스트 응답으로 정확한 장소 ID 매칭, 사진 누락/잘못된 URL, 전체 분류 유지, 설정 및 좌표 오류 응답을 검사했습니다. 브라우저에서는 기존 외식 검색/회전/카테고리 변경을 테스트 응답으로 검사했습니다. 실제 사진 URL 및 운영 서버 배포는 미검증입니다.
